import { MainTemplate } from "../templates/MainTemplate";

const Product = () => {
    return (
        <MainTemplate>
            <div></div>
        </MainTemplate>
    );
};

export default Product;
